<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools" android:id="@+id/nav_graph"
            app:startDestination="@id/racesFragment">

    <fragment android:id="@+id/racesFragment" android:name="ru.kd.formula1.races.RacesFragment"
              android:label="fragment_races" tools:layout="@layout/fragment_races">
        <action android:id="@+id/action_show_top_list" app:destination="@id/topListFragment">
            <argument
                    android:name="season"
                    app:argType="integer"
                    android:defaultValue="-1"/>
            <argument
                    android:name="season"
                    app:argType="integer"
                    android:defaultValue="-1"/>
        </action>
    </fragment>
    <fragment android:id="@+id/topListFragment" android:name="ru.kd.formula1.toplist.TopListFragment"
              android:label="fragment_top_list" tools:layout="@layout/fragment_top_list">
        <action android:id="@+id/action_show_driver_info" app:destination="@id/driverFragment">
            <argument
                    android:name="driver_id"
                    app:argType="string"/>
        </action>
    </fragment>

    <fragment android:id="@+id/driverFragment" android:name="ru.kd.formula1.driver.DriverFragment"
              android:label="fragment_driver" tools:layout="@layout/fragment_driver"/>
</navigation>